# ---- App ----
APP_ENV=development
FRONTEND_URL=http://localhost:3000

# ---- Supabase / Postgres ----
# Leave blank to run with the in-memory fallback store (zero setup).
# Your project URL is pre-filled below; paste the service_role SECRET key
# from Supabase Dashboard -> Settings -> API to enable real persistence.
SUPABASE_URL=https://lfpfdpfktvjpnjwmegyd.supabase.co
SUPABASE_KEY=

# ---- Redis (optional, falls back to in-memory cache if unavailable) ----
REDIS_URL=

# ---- Stripe (leave blank to disable checkout) ----
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ELITE=
STRIPE_PRICE_EDGE=

# ---- Weather (mock service works with no key) ----
WEATHER_API_KEY=
